/*
 * Sorts integers using either the minimum element sort or the 
 * bubble sort algorithm. Takes in integers from the command-line.
 */


#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[])
{
  int i;
  /* 0 refers to bubble sort. 1 refers to minimum element */
  int algo = 0;
  for (i = 1; i < argc; i++)
  {
    if (strcmp(argv[i], "
      
